document.getElementById('codigo').addEventListener('click', function() {
    // URL do PDF que você quer abrir
    var pdfUrl = 'Elements/Curriculum Vitae.pdf';

    // Abrir o PDF em uma nova guia do navegador
    window.open(pdfUrl, '_blank');
});
